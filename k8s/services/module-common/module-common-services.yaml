apiVersion: v1
kind: Service
metadata:
  name: module-common-stable
  namespace: app
spec:
  selector:
    app: module-common
  ports:
  - port: 80
    targetPort: 8011
    protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: module-common-canary
  namespace: app
spec:
  selector:
    app: module-common
  ports:
  - port: 80
    targetPort: 8011
    protocol: TCP
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: module-common-service
  namespace: app
spec:
  selector:
    app: module-common
  ports:
  - port: 80
    targetPort: 8011
    protocol: TCP
  type: ClusterIP
